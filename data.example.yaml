# Copy to data.yaml — path must be the DATASET ROOT (parent of images/ and labels/)
# NOT the images/ folder itself.
path: .
train: images/train
val: images/val
nc: 1
names:
  0: earlobe
kpt_shape: [1, 3]
